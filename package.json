{
  "name": "bessere-schule",
  "private": true,
  "type": "module",
  "description": "Inoffizielle Bessere-Schule-PWA für beste.schule. Statische App, kein Build-Schritt; package.json existiert nur für `type: module` (Node-ESM für scripts/) und npm-Skripte.",
  "scripts": {
    "discover": "node scripts/discover.mjs",
    "cors-check": "node scripts/cors-check.mjs",
    "anonymize": "node scripts/anonymize.mjs",
    "test": "node scripts/test-grades.mjs && node scripts/test-pkce.mjs && node scripts/test-school-day.mjs && node scripts/test-timetable.mjs",
    "serve": "node scripts/serve.mjs"
  }
}
